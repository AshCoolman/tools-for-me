# claude-token-usage-fragile Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-30

## Active Technologies

- Bash (target `/bin/bash` on macOS — Bash 3.2 minimum, no Bash 4+ features) + `curl` (system), `jq` (Homebrew, required at install time), `launchd` (system) (001-claude-usage-scraper)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Bash (target `/bin/bash` on macOS — Bash 3.2 minimum, no Bash 4+ features)

## Code Style

Bash (target `/bin/bash` on macOS — Bash 3.2 minimum, no Bash 4+ features): Follow standard conventions

## Recent Changes

- 001-claude-usage-scraper: Added Bash (target `/bin/bash` on macOS — Bash 3.2 minimum, no Bash 4+ features) + `curl` (system), `jq` (Homebrew, required at install time), `launchd` (system)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
