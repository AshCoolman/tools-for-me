# Objective

{{oneLiner}}

# Context

<!-- TODO(token-smoulder): name every file, command, or external system the agent touches. A fresh agent must not need to ask. -->

# Constraints

<!-- TODO(token-smoulder): explicit Do / Don't list. Justify the riskClass declared in executor.ts (e.g. why readonly, not repo-local). -->

# Done When

<!-- TODO(token-smoulder): one mechanical PASS/FAIL per line, using one of:
  file:<path>             — file exists (path relative to repo root)
  exit:<command>          — running <command> exits 0
  match:<regex>:<source>  — regex matches; <source> is a file path or `$<command>` for stdout
No "looks good", "is complete", "the user is happy". -->
