# Conventions

House conventions live in the repo's `.dev/docs/` as gap-filling TOCs —
gitignored markdown indexes that point out of repo to the shared docs.
Scan the TOC, then open the source path it cites if you need the full text.

- [Code style guide TOC](../../.dev/docs/code-style-guide.gitignored.md) — `../../.dev/docs/code-style-guide.gitignored.md`
- [Front-end guidance TOC](../../.dev/docs/fe-guidance.gitignored.md) — `../../.dev/docs/fe-guidance.gitignored.md` (only relevant if this package ships UI)

If `.dev/docs/*.gitignored.md` is missing, the TOCs were never created on
this checkout — see `x/mono-public-init/README.md` for how to recreate them.
