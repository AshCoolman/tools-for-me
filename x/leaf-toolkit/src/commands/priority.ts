// Stub: `leaf priority`. To be ported from scripts/leaf-priority.mts.
// Logic: interactive TUI that lets the user assign p0..p5 per leaf, writing
// the result to LEAF.priority.md frontmatter.

export async function priority(_argv: string[]): Promise<void> {
  throw new Error("not yet ported — see fe-mono-closed/scripts/leaf-priority.mts");
}
